# Foreign function to print a number to console.
@foreign("io.c", "println")
int println(String msg);

# Each program must have a main function.
# This function is executed automatically.
int main() {
  println("Hello, World!");
  return 0;
}